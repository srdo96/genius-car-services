import "./App.css";
import { Route, Routes } from "react-router-dom";

function App() {
  return (
    <div className="App">
      <Routes>
        <Route to="/" element={} />
        <Route to="/about" element={} />
      </Routes>
    </div>
  );
}

export default App;
